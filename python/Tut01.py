def pytha(a,b,c):
	if (a*a)+(b*b)==(c*c)
		return true
	else if (a*a)+(c*c)==(b*b)
		return true
	else if (c*c)+(b*b)==(a*a)
		return true
	else
		return false

def pytha2(a,b,c):
	return (a*a)+(b*b)==(c*c)||(a*a)+(c*c)==(b*b)||(c*c)+(b*b)==(a*a)

	# // rundet gegen -unendlich
	# -10//3 = -4 = 10//-3
	# & rundet gegen 0
	# 